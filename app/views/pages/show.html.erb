<h2 class="heading"><%= @page.name %></h2>
<%= @page.content.html_safe %>

<% if current_page?(page_path('resume')) %>
 <p>&NonBreakingSpace;</p>   <p><%= link_to "Download my resume", "#{Resume.first.resume_pdf.url}", class: "btn btn-primary"  %></p>
<% end %>